let message1: string = "hello world"
let message2: string = "hello world"

// 默认情况下，可以推导出对应的标识符的类型时，一般情况下是不加的
const name = "lhd"
const age = 18
const height = 1.88

let message3 = `name:${name} age:${age} height:${height}`
console.log(message3);


export {}